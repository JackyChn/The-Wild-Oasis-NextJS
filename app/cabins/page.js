import Counter from "../_components/Counter";

export const metadata = {
  title: "Cabins",
};

async function page() {
  return (
    <div>
      <h1>Cabin page</h1>
    </div>
  );
}

export default page;
